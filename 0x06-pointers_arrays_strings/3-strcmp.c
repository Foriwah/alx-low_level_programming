#include "main.h"
#include <string.h>

/**
 * _strcmp - compare strings
 * @s1: string one
 * @s2: string two
 * Return: return a value
 */

int _strcmp(char *s1, char *s2)
{
	int value;

	value = strcmp(s1, s2);
	return (value);
}

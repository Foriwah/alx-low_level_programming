#include "main.h"

/**
 * add - function to add to numbers
 * @a: first variable
 * @b: second variable
 * Return: Returns a value
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}

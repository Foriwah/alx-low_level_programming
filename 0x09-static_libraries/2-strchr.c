#include "main.h"
#include <string.h>

/**
 * _strchr - locates a charaacter in a string
 * @s: a variable
 * @c: another variable
 * Return: Returns a char value
 */

char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}

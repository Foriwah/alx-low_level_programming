#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _memset - memset
 * @s: a variable
 * @b: a variable
 * @n: a variable
 * Return: return something
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
